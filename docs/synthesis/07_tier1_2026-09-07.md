# Tier 1 completed, Favor coverage restored, C-19 landed — 2026-09-07

Branch `tier1-2026-09-07` off `main` @ `5d7034f` (verified: `main` = `origin/main`; suite 748
green before any change). One commit per item, suite green at each.

## Veracity of the brief — what I checked before implementing

| Brief says | Finding |
|---|---|
| `main` @ `5d7034f`, merged and pushed, 748 green | **Confirmed** (`origin/main` = `5d7034f`; 748 passed, 114 s). |
| "coordinate before editing" the corpus file | It is not under version control (no git root above `consolidated_texts/`). Edited as instructed; a pre-edit copy is in the session scratchpad. |
| Item 5: the course glossary and Sahl's glossary differ on Domain (from `04` A5) | **Not so** — `05` R4 showed the *Course Glossary* (2-22-2021) carries Sahl's Domain, Halb and Hayyiz entries verbatim. "Add a second witness, don't replace" is still the right edit (it is what was done), but its premise is wrong: both glossaries make the halb/hayyiz split. |
| Item 6: "`05` R6 argues `04` under-claims here" | **Misattributed.** `05` R6 is about fn. 213 (spear-bearing's Sun/Moon asymmetry), not C-02. `05`'s finding on C-02 is in its Tier 3 table: the *On Times* endorsement and *Choices* 6, 16–17 are **confirmed**; *Choices* 6, 30 is vocabulary only. That is the disagreement I adjudicated (item 6 below). |
| Item 7: three judgments already reproduce | **Confirmed** — the probe returns exactly `Opposition, Applying, 03° 00'`, `Square, Separating`, WSH 4. |
| "~35% of charts affected" | Not re-derived. |

## 1. Favor & Recompense coverage — `6eebce1`

`tests/conftest.py` `CHARTS` gains `1240-01-04` (Florence, 14:30 LMT like the others).
Verified before committing: Sun at **21.18° Capricorn** (degree 22, a Well degree), Saturn at
27.4° Cancer, its domicile lord, connected by opposition → `evaluate_favor_and_recompense` returns
`[{'Planet': 'Sun', 'Condition': 'Well', 'Favored By': 'Saturn'}]` — the first Saturn-helped Favor
row in the fixtures. 1240-05-26 was not swapped.

`tables.json` regenerated: **77 insertions, 0 deletions**; "Favor & Recompense" appears in both
configuration slots of the new date. Suite: 871 passed (748 + the sixth chart's 123
parametrizations).

## 3. A3, the corpus marker — done first, outside the repo

`abu_mashar_book_vii.md:1790` — `*[Chapter VII.7, "On the casting of the planets' rays," begins on
the following page, which is not included in this photo set.]*` — removed with its trailing blank
line. The VII.7 heading is now at line **1810** (was 1812); earlier artifacts cite 1812. The file
has no remaining "not included in this photo set".

## 2. A1, the coverage entry — `c9032b5`

`app.py` `NOT_IMPLEMENTED_COVERAGE`, "Abu Ma'shar VII.7". Before: *"The casting of rays according
to Ptolemy. The chapter begins on a page not photographed."* After: the chapter is complete at
1–22 (pp. 485–487); what is not in the corpus is the tables it presupposes — *"the 'hourly times'
(Gr. hōriaioi chronoi) … special tables in the Almagest"* (fn. 250) and *"old tables in which the
ascensions and zodiacal degrees are converted into their opposites"* (fn. 251), both computable;
plus 22 verbatim — *"as for the opposition, [a planet] casts its ray into the opposition of its
sign, in the same degree and minute"* — and the 18/21 asymmetry (left rays: *"add the result to that
one of the two positions which is nearest the planet"*; right rays: *"add it to the more distant of
the two places"*). Worded so that the existing `ABSENCE_CLAIM` test does not misfire: the entry
cites paragraphs, not "VII.7", inside the description.

## 4. A2, the test — `88b1d6a`

`tests/test_prose_counts.py`. Diagnosis followed: `05`'s (stage one). Two tests:

- `test_coverage_entries_do_not_call_a_photographed_passage_missing` — `PAGE_ABSENCE` matches
  *not photographed / photo set / page not / begins on a page / not legible / not included /
  illegible / missing page / not yet photographed|scanned*; the book is read from `passage + desc`.
  Needs no corpus file, so it runs in CI. **Shown red against the pre-fix entry:**
  `AssertionError: Abu Ma'shar VII.7: says its text is unphotographed/missing, but Book ['VII'] is
  complete in the corpus`.
- `test_page_absence_claims_agree_with_the_corpus_when_present` — skipped when
  `~/Desktop/…/consolidated_texts` (or `$CORPUS_DIR`) is absent; otherwise checks the
  `### Chapter VII.N` heading for every VII passage cited, so a future marker in wording the regex
  does not know is still caught. Also red before the fix, green after.

Why not `passage + desc` for the *existing* `ABSENCE_CLAIM` test, as `04` proposed: the control
entry "VII.6, 13 and 36 … No table for them in this corpus" cites VII in its passage and is
*correct*; widening that test would fail it. Page-absence and table-absence are different claims
and now have different tests.

## 5. A5, the glossary citations — `caa2309`

Six sites (`grep -in glossary`). Sahl's glossary is cited with its Vol. I page at five of them,
the course glossary retained:

| `app.py` (post-edit) | Entry | Sahl's glossary |
|---|---|---|
| Domain (in `evaluate_accidental_dignities`) | **added** as second witness, with the Halb/Hayyiz split quoted: halb *"for diurnal planets, when they are in the same hemisphere as the Sun"*; hayyiz *"technically equivalent to halb, except that the planet is also in a sign of its own gender"* | p. 777; p. 782 |
| advancing docstring (83) | *"dynamically angular or succeedent, i.e. moving by primary motion toward an axial degree"* | p. 771 |
| cadent / aversion (66, 91) | *"3rd, 6th, 9th, 12th. But see also falling away from, which is equivalent to Aversion"* | p. 774 |
| advancement definition (83) | same p. 771 sentence | p. 771 |
| eastern (84) | sense (2) *"to be outside the Sun's rays and visible (eastern) or under them and invisible (western)"* | p. 778 |
| "Glossary makes the same equivalence under Cadent" (returning, 66) | **left as is**, per the brief. Note: the two glossaries' Cadent entries are word-for-word identical, so the question "which glossary" has no consequence. | — |

## 6. A4 / C-02, the advancing comment — `6d6687b`

Extended the block above `ADVANCING_BY_QUADRANT_FIG90` with *On Times* Ch. 1, 10–14 (the quadrant
scheme "according to the statement of the ancients"; Māshā'allāh's hemispheres, "he differs from
them"; ¶14 *"And that is the closest of the two statements, in [its] analogy"*, fn. 7), Figure 44,
and *On Choices* Ch. 6, 16–17 (*"the highest region of the circle"* / *"the lowest part of the
circle"*). No computed value moves; `ADVANCING_BY_QUADRANT_FIG90` and Sahl 83 are byte-identical.

**Where `04` and `05` differ, I followed `05`:** `04` A4 says Sahl "uses" the hemisphere scheme at
*Choices* 6, 16–17 **and 6, 30**; `05` (Tier 3) reads 6, 30 — *"let the lord of the Ascendant be
declining from the Midheaven toward the stake of the earth"*, depilation advice — as sharing the
vocabulary only. The comment cites 16–17 as the use and names 6, 30 as vocabulary. The caveat both
agree on travels with it: Ch. 1 is about quickness of *timing*, Ch. 6 about a bodily
correspondence; neither licenses a third *strength* scheme, so nothing is scored.

## 7. C-19, Sahl's worked question chart — `427629e`

`tests/test_sahl_question_chart.py`: 19 passed, 2 strict xfails. Inputs are ¶54's longitudes as
literals; the date is never used. Assertions, each with Sahl's sentence in the test:

| ¶ | Assertion | Engine call |
|---|---|---|
| — | nocturnal: Sun in the 2nd sign, below the horizon | `get_wsh_house(102, 80) == 2` |
| 56 | Mercury lord of the Ascendant, in it, "at the end of the sign" (read as the final bound, Saturn's) | `get_essential_rulers`, `get_wsh_house == 1`, `_in_last_bound(87)` |
| 56 | Jupiter lord of the sought matter, in the Midheaven | Pisces lord Jupiter; `get_wsh_house(350, 80) == 10` |
| 56 | lord of the Asc *separated* from the lord of the sought matter | aspects row `('Square', 'Separating')` |
| 57 | Moon in the stake of the earth | `get_wsh_house(167, 80) == 4` |
| 57 | Moon connecting with Jupiter from an opposition | `('Opposition', 'Applying', "03° 00'", 'Yes')` |
| 63 | Mercury shifts from his house into the house of assets | Cancer = 2nd |
| 63 | on leaving, connects with Mars, "who does not accept him" | Cancer ∈ `FALLS['Mars']`; under Sahl's profile `evaluate_non_reception` → **Kind II (59–60), Mercury → Mars** — the engine's name for Questions Ch. 1, 40's own example, *"the lord of the Ascendant is connecting with Mars from Cancer"*. Mercury placed at 1.5 Cancer, inside his 7° light of the sextile |
| 65 | Jupiter with the Tail | ☋ hardcoded 25♓46′ from Figure 34 (true node), same-sign only |
| — | **Venus control**: every non-Venus aspect row identical at 3♌ and 9♌06′; every test parametrised over both | |

**Bounds check requested by the brief:** the correction changes one lordship in this chart — Saturn
at 6♊ moves from Venus's bound to Jupiter's — and no judgment touches it; Mercury 27♊ is in
Saturn's bound under both tables. Asserted in the fixture.

**A finding, not fixed (out of scope):** for the ¶63 pair the engine, under Sahl's own profile, also
reports *"Mars receives Mercury via triplicity, bound"* (Māshā'allāh's form, *Introduction* Ch. 3,
54–55 — Mars is the night triplicity lord of Cancer and holds its first bound) **alongside**
non-reception Kind II. Sahl's verdict on his own chart is *"he does not accept [Mercury]"*, and
Ch. 1, 40–41 makes connection from the receiver's fall a refusal. Which rule wins when both fire is a
doctrinal decision the engine has not made. Recorded as a **strict xfail**
(`test_63_control_the_engine_does_not_also_report_mars_receiving_mercury`) so the suite flips
loudly when it is decided. Under Abū Ma'shar's profile the same reception row appears and there is
no fall veto at all.

Also not asserted: ¶61 "stationing toward retrogradation" (a stated fact, entered as Jupiter's
near-zero speed, not derivable from the longitudes); ¶58's counterfactual ("if they had connected
from a trine or sextile").

## Could not verify

- The 300-dpi Handy Tables read remains mine (unchanged from `06`).
- Whether the OCR project has any process that regenerates `abu_mashar_book_vii.md` and would
  reinstate the marker; the file is not versioned.
- The brief's 35% figure.
