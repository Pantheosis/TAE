# Desktop launcher and PyInstaller build only (desktop_launcher.py, icon.py,
# build.spec). Everything the app itself needs comes from requirements.txt, so
# this file includes it rather than restating the pins -- one place to change a
# runtime version, and no way for the built binary to ship a different
# Streamlit than the tested one.
-r requirements.txt

# Pinned from Executable/.venv, the venv the desktop build uses.
pywebview==6.2.1
pillow==12.3.0

# Windows-only, and DELIBERATELY UNPINNED. These have no Linux install to read
# a version from -- pywebview's Qt backend uses PyQt6 here and PySide6 there --
# so pinning them would mean inventing numbers. Take the versions from the
# "Install dependencies" step of the next Actions run and pin them here.
PySide6; sys_platform == "win32"
qtpy; sys_platform == "win32"
